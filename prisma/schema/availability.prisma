

model Availability {
  id        String @id @default(uuid())
  day       String
  startTime String
  endTime   String

  tutorId String
  tutor   TutorProfile @relation(fields: [tutorId], references: [id])

  @@map("availability")
}
